import type { ProductionRecord, RoundingMode } from '../types';

export function processRawData(
  records: ProductionRecord[],
  roundingMode: RoundingMode
): ProductionRecord[] {
  // TODO: hier kommt später deine echte Logik rein
  return records ?? [];
}
